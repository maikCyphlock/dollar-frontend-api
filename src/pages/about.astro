---
import "@fontsource/space-grotesk";
import Layout from "@layouts/Layout.astro";
---

<Layout title="why dolar api?">
    <section class="about">
        <div class="about-header">
            <img class="about-img" src="./dolar.png" alt="" />
            <h1 class="about-heading mt-1">what is dolar api?</h1>
        </div>

        <div class="about-description">
            <p>
                This is an API for developers to see fluctuation in dollar
                pricing compare to Venezuelan bolivar sovereign currency.
            </p>
            <p>
                It uses a scrapper to extract currency exchange rates from
                different sources and calculates the average for each day.
            </p>
        </div>
    </section>
</Layout>
<style>
    .about-header {
        overflow: hidden;
        position: relative;
        margin-top: 1rem;

        background-color: rgb(249, 234, 167);
        padding: 4rem;
    }
    .about-heading {
        font-family: "Space grotesk", serif;
        grid-area: 1 / 1;
        position: relative;
        text-transform: uppercase;
        font-size: 4rem;
        z-index: 11;
    }
    .about-img {
        z-index: 1;
        position: absolute;
        top: -70px;
        right: 0;
        width: 400px;
        opacity: 0.5;
    }
    .about-description {
        line-height: 1.5rem;
        padding: 1em;
    }
    .about {
        padding: 1rem;
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    html.dark {
        & .about-heading {
            color: var(--black-trans-75);
        }
    }
</style>
